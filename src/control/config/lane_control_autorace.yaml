#config/lane_control_autorace.yaml

# ── PID 게인 (lateral_offset [px] → steering) ──
kp: 0.01
ki: 0.0
kd: 0.005
i_max: 50.0

# ── 조향 제한 ──
max_steering: 0.5

# ── 속도 ──
base_speed: 0.5
v_min_curve: 0.10
curv_slow_thresh: 0.003
curv_slow_gain: 50.0

# ── 미검출 처리 ──
lost_timeout: 1.0

# ── 제어 주기 ──
control_rate: 25.0
